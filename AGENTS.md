# 汉末群雄传 AI - 开发与维护审计清单 (Audit Checklist)

## 1. 核心安全原则：数据完整性
- **严禁静默删除**：在任何非手动保存触发的情况下，严禁使用正则表达式剥离或删除 `gameState.history` 中的原始文本内容。
- **UI 渲染与原始数据隔离**：渲染层（StoryDisplay）的“噪音过滤”只允许隐藏标签标记，绝不允许通过 `replace` 永久删除原始消息中的任何叙事信息。
- **全量显示兜底**：渲染逻辑必须处理标签外的“缝隙文本”，确保 AI 输出的所有叙事信息（如政令影响、战事总结）即使没加标签也必须高亮显示。

## 2. 史实严谨性与幻觉防御 (Historical Rigor & Antihallucination)
- **史实锚点原则**：AI 输出必须严格区分 `<narrative>`（叙事）与 `<system_state>`（结构化数据）。
- **历史记录净化**：在将历史消息发送回 AI 前，必须移除旧消息中的冗余 JSON 块（如 `HISTORICAL_SYNC`），保留叙事文字，防止 AI 产生状态幻觉。
- **名实相符审计**：当 AI 在叙事中提到的武将官职、势力版图与状态标签中的数值不一致时，必须以标签内的结构化数据为准。
- **身份属性锁定审计**：严禁 AI 将玩家（马靖）的固定属性数值与 NPC（如刘备）混淆。审计时必须核对：UI 面板中的数值是否被 AI 错误地归类到了 NPC 名下。
- **严禁魔法渗透**：审计逻辑必须定期检查是否出现了“法术”、“咒术”等超自然或仙侠遗留词汇，一旦发现必须立即溯源并清除提示词中的残留。
- **时代局限审计**：严禁出现汉末以后（唐、宋、明、清及现代）的文学、名句引用（如林则徐、岳飞等的诗词）或不合时宜的成语。一旦发现 NPC 表达超越时代，需判定为逻辑崩溃并进行拦截修正。

## 3. 性能优化 (Latency & Token Usage)
- **势力上下文剪裁**：过多的郡县监控或无关势力数据会消耗大量 Token。需优化省份监控（Province Monitor）逻辑，仅同步与当前地区相关的变动。
- **增量更新协议**：AI 只需同步变动的武将属性或势力数值，无需每回合全量下发。

## 4. 沉浸感与 Token 效能审计 (Immersion & Token Efficiency)
- **严禁思维链泄露**：严禁在输出正文中包含任何规划、自白或英文标题。
- **叙事密度优先**：每一回合的输出必须以高质量的中文叙事为核心，字数控制在800字以上，多描写风景、时节、天候、人文环境等背景因素，烘托乱世氛围，严禁使用简短的网文流派流水账。
- **去主角中心化与独立动机**：审计时严格检查NPC是否具备独立的动机、欲望和行事主控权。严禁NPC的所有焦点或矛盾无脑围绕玩家流转（去主角神格化、无端仇恨与工具人退场逻辑）。
- **严禁技术日志**：`[LOG_DATA]` 等中间数据绝对不准出现在最终输出中。

## 5. 九品中正与门阀审计 (Social Logic Audit)
- **出身一致性**：检查 AI 是否在叙事中正确体现了士族与寒门的政治壁垒。
- **地理距离校验**：定期核查行军时间是否符合汉末地理常识。
